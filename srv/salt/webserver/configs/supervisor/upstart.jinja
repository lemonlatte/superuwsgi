{% set supervisor = pillar.get('supervisor', {}) -%}
# Ubuntu upstart file at /etc/init/supervisord.conf

start on runlevel [2345]
stop on runlevel [06]

respawn

exec `which supervisord` --nodaemon -c {{ supervisor.get('config_file', '/etc/supervisord.conf') }}
